extends ../layout

block content
    h1.
        #{title}
    form#formAddProfile(name="addprofile",method="post",action="/profiles")
        p Username: 
            input#inputUsername(type="text", placeholder="username", name="username")
        p email:
            input#inputEmail(type="text", placeholder="name@mail.com", name="email")
        p Password: 
            input#inputPassword(type="password", name="password")
        p DeviceId: 
            input#inputDeviceId(type="text", name="deviceId")
        p
            button#btnSubmit(type="submit") submit